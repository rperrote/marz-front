import { test } from './fixtures'

test.describe('Inbox happy paths', () => {
  test.skip('TODO: creator accepts an offer inline from inbox when backend dev exposes an Inbox offer seed', async () => {})

  test.skip('TODO: brand sees the empty inbox state when backend dev exposes a deterministic empty Inbox seed', async () => {})
})
